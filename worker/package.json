{
  "name": "vaultid-upload-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that gates VaultID encrypted-bundle uploads on CLAWD allowance and forwards to Pinata.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.0",
    "viem": "^2.21.0",
    "wrangler": "^3.78.0"
  }
}
